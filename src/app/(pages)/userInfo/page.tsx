import React from 'react';
import UserDetails from "@/app/components/UserDetails";

const Page = () => {
    return (
        <div>
            <UserDetails/>
        </div>
    );
};

export default Page;